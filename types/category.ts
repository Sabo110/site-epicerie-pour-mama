export type CreateC = {
    name: string
    visible: boolean
    slug: string
}

export type C = {
    id: number
    slug: string
    name: string
    visible: boolean
    createdAt: Date
    updatedAt: Date
}